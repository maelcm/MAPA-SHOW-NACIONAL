# No Render: use a variável de ambiente GCP_SERVICE_ACCOUNT_JSON (Settings → Environment).
# No Streamlit Cloud: configure os secrets no dashboard do app (substituem este arquivo).
# Este arquivo existe apenas para o Streamlit não exibir "No secrets found" ao iniciar.
